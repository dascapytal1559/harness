# feedbacks

One file per council run, written by the judging agent, about the ai-council skill itself, never about the subject. The posting rule and the file shape are the "Feedback" section of `SKILL.md`. Nothing here is applied automatically; a human reads these when refining the skill.

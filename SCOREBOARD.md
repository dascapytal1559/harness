# Model scoreboard

Points are awarded by the user, never by the model being scored. One point, one reason. Do not collapse, average, or retroactively reweight.

When the user awards a point, append a ledger row and update the totals. Use the model's full id (`grok-4.6`, not `grok`).

## Totals

| Model | Points |
| --- | ---: |
| grok-4.6 | 1 |

## Ledger

| Date | Model | Delta | Reason |
| --- | --- | ---: | --- |
| 2026-08-13 | grok-4.6 | +1 | good design: nearest-neighbor effort mapping, missing tiers clamp to the closest supported rung |
